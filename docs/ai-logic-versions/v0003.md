# v0003 — Issue #126 STATUS 실험의 최신 main 통합 (test 전용)

## 변경과 복원 기준

- 기록일: 2026-09-11. 사용자 요청에 따라 Codex가 통합했다. 구현 PR은 아직 만들지 않았다.
- 이전 버전: v0002. 기준 main: `05d9c2d65498d28a1208b7f9bc8f0087307f64fc`.
- 관련 이슈: [#126](https://github.com/catchhole-soma/catchhole-backend-ai/issues/126).
- 복원 기준 제품·평가기 코드 SHA: `d7a1fd5e6acce6bce3a8836cda9e4922c6a54cac`.
- 대상 브랜치: `test/gh-126-status-stability`. main에 병합하거나 운영 배포하지 않았다.
- 출처: 로컬 `.worktrees/gh-126-ai`의 `fix/gh-126-status-stability`, HEAD `868d359076b4acc80304f24401966332def102d1` 위의 **미커밋 작업**. 이 HEAD만으로 실험을 복원할 수 없으며 위 통합 SHA를 사용한다.
- 원본 gh-126 작업과 기존 `test/world-setting-name-evaluation`은 변경하지 않았다. Java 저장소의 `.worktrees/gh-126-java` 실험은 이 AI 브랜치에 포함되지 않는다.

## 통합 범위

- STATUS 전용 관찰 추출, 원문 근거 참조·검수, 상태 종료 검수와 제안 값 검증을 이식했다.
- 이전 회차 문맥, 앞 청크 관찰, 회차 전체 동일인 검수를 Worker와 다단계 평가기에 연결했다.
- 최신 main의 공통 후보 준비 및 처리 진단을 유지하면서 STATUS 사건별 중복 제거와 저장된 인물 이름 사용을 적용했다.
- 원본 평가 도구·합성 fixture와 과거 결과/비용 문서를 보존했다. 과거 결과는 **이번 통합 코드의 성능 측정이 아니다**. 원시 build 산출물과 비공개 원문은 포함하지 않았다.
- 제품 모델 기본값은 main에서 유지한다. 주요 cache key는 `setting-extraction:v11`, STATUS 추출 `status-extraction:v11`, `subject-resolution:v5`, `character-identity-resolution:v3`, `status-observation-review:v9`, `character-fact-comparison:v10`, `character-fact-comparison-batch:v5`, `character-status-lifecycle:v2`다. STATUS 추출 키의 schema suffix는 코드에서 생성한다.

## 검증 및 평가

- 로컬 `python -m pytest -q --tb=short`: **1910 passed, 2 skipped**, Starlette 의존성 경고 1개. 코드 커밋 전 동일 파일 상태에서 실행했다.
- 전체 ruff 검사는 기존 코드/테스트 경고가 남아 통과하지 않았다. 이번 추가 평가 경계의 포괄 예외 처리는 실패 진단 보존을 위한 명시적 예외다.
- fixture 바이트 보존으로 기존 합성 파일 일부의 EOF 빈 줄 경고는 유지했다.
- 실제 모델 품질 평가: **미측정**. 새 유료 모델 호출이나 Actions 평가를 실행하지 않았다. 점수·fixture hash·실행 모델·run URL은 아직 없다.
- 재평가: 사용자가 Actions 다단계 평가에서 위 test 브랜치를 선택해 실행한다. 기본 FIXED 및 Sol 추출/Terra 주체 해소/Sol 비교, medium을 사용하되 실제 실행 입력과 결과는 실행 후 추가 기록한다.
- 캐릭터 1차 P/R/F1·값 정확도, 2차 처리/전체 결정 정확도, 최종 상태 F1 및 미판정/실패 수: 모두 미측정. 과거 실험의 실패가 해결됐다고 주장하지 않는다.

## 복구·배포 제약

운영 배포는 미실행이다. AI Actions 평가는 이 브랜치로 가능하지만 Java를 포함한 실제 순차 저장·확정 평가는 별도 gh-126 Java 변경과 평가 전용 환경이 필요하다. 새 `previousEpisode` 입력은 선택 사항이며 미제공 시 이전 회차 원문 문맥은 없다. 저장 결과는 코드 복원만으로 되돌아가지 않는다. main 적용 전 Java 호환성과 실제 품질을 별도로 검증해야 한다.
