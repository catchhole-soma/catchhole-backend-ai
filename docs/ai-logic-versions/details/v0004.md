# v0004 상세 — main 기반 연속 분석 (#65 제외)

## 현재 기준: PR65 제외 (2026-09-13)

- 사용자 결정에 따라 #70을 main 직접 대상 PR로 분리했다. #65의 선행 머지나 코드는 필요하지 않다.
- 기반 main: `05d9c2d65498d28a1208b7f9bc8f0087307f64fc`.
- 현재 코드 복원 SHA: `6b881363d1624cfea1d8552aff8cb6aec18b32dd`. 뒤따르는 기록 커밋은 문서만 변경한다.
- 제외한 #65 head: `6deac0400c7b84a22715d669f30dfb12a7ca2b34`. 기존 통합 head `12d915a6ad790416b96911493c24d0cb866489ba`는 로컬 `backup/gh180-with-pr65-20260913`에 보존했다.
- #65 head와 기존 #70 사이의 차이만 main 위에 적용하고, 세계관 비교 prompt의 #65 분류 보완 문구도 제거했다. #65의 종족 전투 특성 묶음·능력/세계 규칙 분류·보완 설명 병합 지침은 포함하지 않는다.
- 세계관 추출 prompt는 main과 바이트 단위로 동일하며 추출 cache는 main의 v2다. 비교 cache는 과거 통합본과 구별하는 단건 v12·batch v9, ordered suffix는 기존 `:ordered-provisional-v6`를 유지한다.
- 기존 통합본 대비 런타임 차이는 세계관 추출·비교 prompt와 cache, prompt 안내 문서, 평가기의 main 추출 cache 기록 방식뿐이다. 저장·재개·부분 복구·오류 격리 구현은 기존 리뷰 완료 코드와 동일하다.
- main의 PR63·64·67 변경은 유지한다. PR65의 v0003 및 기존 문서 구조 재편은 가져오지 않으며 main의 v0001·v0002·PR 양식·버전 규칙은 유지한다. 번호 충돌을 피하기 위해 이 PR의 v0004를 유지한다.

### 분리 검증

- 코드 커밋 기준, 로컬 환경변수/자격 증명을 비운 Python 3.12 환경에서 소켓을 차단해 `python -m pytest`에 해당하는 전체 수집을 실행했다: **1,879 통과 / 외부 DB 14개 skip**, 기존 Starlette deprecation 경고 1개. `pytest -m 'not integration'`도 **1,879 통과 / 14 제외**다.
- `ruff check app evals tests scripts` 통과. 새 schema/API/중단 조건은 추가하지 않았다.
- `tests/fixtures/upstream_requests_main_05d9c2d.json`은 main SHA의 독립 Git archive에서 두 고정 probe로 직접 포착한 6종 요청이다. 세계관 1차 요청 전체가 동일하고, 나머지는 GH180 자연어 이유·주체 연결·일반 검토 prompt와 비교 cache만 다르다. 모델·reasoning·store·user payload·schema·출력 상한은 동일하다.
- `expected-reports-main-05d9c2d.json`은 같은 archive의 채점기로 고정 합성 예측을 평가한 ORACLE/FIXED/ROLLING 전체 보고서다. 현재 코드와 모든 필드가 같으며 이전 PR65-6deac04의 보고서와도 같다. #65는 이 저장 입력의 채점기를 바꾸지 않았으므로 점수 개선을 뜻하지 않는다. 이전 fixture는 역사 자료로 보존한다.
- 실제 원고·Gold·judge를 이용한 새 품질 평가, 외부 provider 호출, 새 PostgreSQL/Spring 통합 실행은 하지 않았다. 앞선 실제 20+10화와 통합 21개 결과는 아래 **분리 전 코드의 이력**이다. 현재 코드의 실제 연속 실행 증거로 재사용하지 않는다.
- 로컬 증거: `.codex-artifacts/gh180/pr65-separation/{audit.json,upstream-capture.json,full-pytest.log}` 및 `separation-final-tests.log`. 재현 입력은 커밋된 합성 fixtures와 두 request probe다.
- 정확도·재현율 미측정의 사유는 이번 범위가 #65 분리와 연속성 회귀 확인이기 때문이다. GH180 담당은 다음 승인된 실제 연속 실행을 확인하고, 정확도 담당은 별도 이슈에서 동일 Gold·모델·judge의 FIXED 평가를 수행한다.
- Java #193의 V43~V53/API를 먼저 반영한 뒤 AI를 배포한다. 현재 로컬 실행 프로세스·사용자 DB·운영은 변경하지 않았다.

## 이하: 분리 전 통합·실행 이력

아래 SHA와 실행 수치는 당시 #65를 포함한 코드의 기록이다. 현재 브랜치의 조상 또는 현재 검증 결과라는 뜻이 아니며, 복원은 위의 현재 코드 SHA를 사용한다.

## 적용 범위와 복원 근거

- 날짜: 2026-09-11, Asia/Seoul. 상태: 커밋 전 로컬 통합 검증, 운영 미배포.
- 관련 작업: [Java #180](https://github.com/catchhole-soma/catchhole-backend-java/issues/180), [AI #65](https://github.com/catchhole-soma/catchhole-backend-ai/pull/65).
- GH180 기준 HEAD: `80d5184c006a42248ab214b36d0898c071887900`. 기존 미커밋 변경을 보존했다.
- 함께 반영한 AI main: `2dde1839616ecdc584b7f68e5bb33f3f5f181a29` (#63, #64 포함).
- 함께 반영한 #65 head: `275bada00fcd4d420277d378ddf7f6d417dea899`. 확인 시 OPEN/DRAFT였다.
- Git merge 완료 커밋을 만들지 않고 파일별 3-way 통합을 적용했다. 실제 전체 실행 트리를 복원할 단일 SHA는 아직 없다. PR 작성 시 변경을 커밋하고 이 항목에 최종 SHA를 추가해야 한다.
- 로컬 통합 영수증·적용 전 파일·파일별 SHA-256: 상위 작업공간 `.codex-artifacts/gh180/pr65-integration/{applied.json,before-manifest.json,before/}`. 이후 보완 내역은 같은 폴더 보고서와 테스트 로그에 기록한다.
- v0002를 포함한 기존 평가·요청 기록은 보존했다. main과 #65의 최종 머지 상태에 따라 버전 번호·복원 SHA를 PR 제출 전에 다시 확인한다.

## 결과에 영향을 주는 변경

1. #63의 캐릭터 STATUS 종료 판단과 평가기 변경, #65의 종족 전투 특성 분류·보충 설명 병합·능력과 세계 공통 규칙 구분을 GH180에 반영했다. #64의 평가 기본값과 기록 체계도 반영했다.
2. 같은 원본 속성의 후보를 합치는 공통 지침과, 범위가 다른 후보를 개별 검토로 남기는 순차 분석 계약의 우선순위를 명시했다. 검토가 필요한 후보는 각각 원본값·근거를 보존하고, 독립적인 정상 후보는 함께 처리할 수 있다. 검증 조건을 완화하거나 오류를 정상 판단으로 바꾸지 않았다.
3. 개별 복구 비교에서는 일반적인 분류명 정리보다 원본 경로 보존을 우선한다. 종족 분류 안내만으로 범위 없는 속성 하나를 새 하위 분류로 이동하거나 존재하지 않는 형제 속성을 만들도록 유도하지 않는다.

변경 위치는 `app/analysis/ordered_world_batch_contract.py`, `world_setting_comparator.py`,
`app/llm/prompts/world_setting_comparison_batch.md`다. 기존 정확한 속성 선택, 제한된 재시도,
독립 후보 복구, lease·quota·입력 크기 오류 경계와 회차별 저장 순서는 유지한다.

## 평가기 통합 보완

새 평가기는 현재 캐릭터 사실의 자료형도 비교한다. Java journal v1에는 자료형이 없어
단순 replay만 하면 실제 값이 같아도 평가 DTO의 자료형이 누락되는 문제가 생겼다.
`ordered_runtime_adapter.py`에서 실제 시작 snapshot/schema의 자료형 목록과, seal된 쓰기에
대응하는 실제 1차 후보의 자료형을 평가 DTO에만 전달한다. Gold·미래 회차·JSON 모양으로
자료형을 추측하지 않는다. 누락·충돌은 명시적으로 거절한다. 검토·이력 전용·미완료 후보는
현재 설정의 자료형을 갱신하지 못하며 Java 원본 journal과 hash는 바꾸지 않는다.

## 요청·채점 회귀 기준

- `tests/fixtures/legacy_requests_80d5184.json` 등 과거 원본 capture를 유지했다.
- `upstream_requests_pr65.json`은 고정된 #65 SHA를 별도 Git archive로 열어 mock transport에서 직접 포착한 요청 기준이다. 원고나 외부 LLM 전송은 없다.
- 추가 `upstream_world_request_probe.py`로 세계관 추출·단건 비교도 확인했다. GH180의 승인된 자연어 이유/주체 연결/범위 우선순위 안내와 cache 차이만 별도 override로 기록했다.
- 과거 저장 예측 입력과 `expected-reports.json`은 그대로 두고, 동일 입력을 실제 #65 채점기로 채점한 `expected-reports-pr65.json`을 별도 보관한다. 점수 변화에 맞춰 과거 기준을 덮어쓰지 않는다.
- 실제 provider model/reasoning/user payload/schema/output limit을 변경한 것으로 간주하지 않도록 요청 회귀 테스트에서 함께 검증한다.

프롬프트 cache: 캐릭터 단건 v10·batch v3, 세계관 추출 v4, 세계관 단건 v10,
세계관 batch v7와 ordered suffix `:ordered-provisional-v5`.
세계관 batch v7는 #65 v6 위의 범위 우선순위 명시를 구분한다.

## 검증 조건과 결과

실제 소설의 유료 품질 평가와 20화 연속 provider 실행은 이번 통합 확인에서 실행하지 않았다.
Gold·dataset version·fixtureHash·회차별 정답 분모·judge 조건은 품질 측정이 없어 해당 없음이다.
아래 테스트의 합성 데이터 점수를 제품 정확도 개선 수치로 보고하지 않는다.

| 확인 | 결과/근거 |
| --- | --- |
| AI 오프라인 전체 테스트 | 1,703개 통과, 외부 연동 14개 제외, 기존 Starlette deprecation 경고 1건. 로컬 `full-final.log` |
| Java 비교/자동 반영 회귀 | `OrderedWorldSettingWorkerTest` 28개, `WorldSettingAutomaticApplicationTest` 30개 통과 |
| 자료형 metadata·hash 계약 | 초기 metadata 누락/충돌/잘못된 참조 거절과 원본 hash 보존 확인 |
| Python editable build | 임시 경로에 build 도구와 설치 산출물을 격리해 성공. 공유 가상환경은 변경하지 않음 |

오프라인 실행은 소켓 연결을 차단한 runner로 `pytest -m 'not integration'`을 실행했다.
Java는 해당 두 테스트 클래스를 지정해 실행했다. 상세 명령·로그는 통합 보고서에 있다.

## 로컬 실행 설정과 다음 확인

기존 로컬 실행 모델은 추출 `gpt-5.6-terra`, 주체 해소·비교 `gpt-5.6-luna`,
추론 강도 `none`으로 유지한다. #64의 **품질 평가 기본값**인 FIXED/Sol·Terra·Sol/medium을
로컬 서비스 설정에 자동 적용하지 않았다. 분석 워커는 2개 × 동시 Job 10개이며,
캐릭터/세계관 재비교 워커는 각각 1개다. 같은 묶음의 회차는 저장 완료 순서로 진행한다.

품질 지표는 미측정이다. 사유는 이번 범위가 로컬 통합 및 연속 분석 안정성 준비이기 때문이다.
후속 담당은 GH180 작업자이며, 사용자가 시작할 20화 분석에서 중단·개별 검토·자동 저장·
다음 회차 입력을 확인한다. 정확도 평가는 별도 담당 이슈에서 동일 Gold·입력·모델·judge
조건으로 수행한다. v0002에 기록된 상태 반영 오류 2건이 이번 변경으로 해결됐다고 주장하지 않는다.
Java/front 최신 main의 별도 변경과 migration 충돌 해소는 이번 AI 통합 범위에 포함하지 않았다.

로컬 기동 확인: 2026-09-11 17:25 KST, 사이트 HTTP 200·API UP, 워커 4개가 현재 코드와 일치했다.
실행 코드 hash: `9a5ce436c149f4f8ff90abe3a49b9fb39b52926c79b24409e2233fbc405def56`.
기존 분석 80건의 상태·완료 시각, 회차 82개의 식별자·번호·수정 시각, 후보·설정 건수를 비교해
변경 없음을 확인했다. 이는 모든 DB 컬럼의 내용 hash 대조를 수행했다는 뜻은 아니다.

## 추가 실행 — 테스트3 1~20화, 대상 연결 중단 수정

2026-09-11 18:27 KST 최종 확인. 앞선 통합 준비 실행과 별개로, 사용자의 명시적
OpenAI 전송 동의를 받은 후 새 로컬 작품의 1~20화를 하나의 순차 자동 분석 묶음으로 실행했다.
시작 입력은 실제 저장된 첫 회차 입력이 빈 상태임을 검증했다. Gold 또는 과거 작품의
설정을 시작 기준으로 가져오지 않았다. 입력은 20개 파일, 공백 포함 127,486자다.

- 실행 ID: 작품 `306d2ed4-0556-4ca6-8e78-7e5f663bccde`, 묶음 `1ce61bb5-fdfe-4e0a-85d7-221b35fb782a`.
- 모드: `ORDERED_PROVISIONAL` + `AUTOMATIC`, 같은 묶음은 회차별 반영 완료 후 후속 claim.
- 실제 공급자 사용 이력: 추출 `gpt-5.6-terra`, 주체 해소·비교 `gpt-5.6-luna`, reasoning `none`. Judge 미사용.
- 원문 snapshot 식별값·파일별 hash 및 최초 실패·재개·검증 기록은 상위 작업공간의 비공개 `.codex-artifacts/gh180/test3-episodes-1-20/`에 보관한다. `input-manifest.json`, `failure-16-before.json`, `resume16-before.json`, `resume16-receipt.json`, `resume16-verification.json`이 실행 근거다. 원문·개별 예측은 이 버전 문서에 복사하지 않는다.
- 최초 실행 코드 hash는 위 `9a5ce436...`; 수정 후 워커 hash는 `49e0bf3bad43b3ef4bf5cbf1a429cb8954c7b457689c1926bfa39cb341b83828`이다. 계산 대상은 로컬 launcher의 `worker_code_hash()`에 명시된 실제 앱·프롬프트·실행 파일이다. Git 복원 SHA가 아니며 최종 커밋 SHA는 앞의 복원 항목에 후속 보완한다.

**실제 중단 1건:** 16화 세계관 주체 연결에서 schema·허용 ref·중복 검증을 통과한 응답이
`ambiguous=true`와 유효한 선택 ref를 동시에 반환했다. 기존 validator는 이를 세 번 거절한 뒤
회차를 `COMPARISON_VALIDATION_FAILED`로 중단했다. 저장된 오류 origin은 수정 전
`WorldSettingSubjectResolver.select_ordered_subjects`의 validate 259행이다. 실패 응답 원본을
보관하지 않아 실제 선택 ref는 확인할 수 없다. 성공한 재개 응답으로 이를 대체해 추정하지 않는다.

**수정:** `world_setting_comparator.py`에서 명시적 ambiguity를 우선해 선택을 비우고 보류를 반환한다.
Java의 기존 ambiguity 우선 처리와 맞춘다. 참조·schema 검증을 먼저 유지하고, 대상 연결·
신규 anchor 생성·대상 근거 누적을 하지 않는다. 알 수 없는/중복 ref와 quota·lease·입력·
provider·Spring 실행 오류를 정상 보류로 바꾸지 않는다. 모델·system/user prompt·schema·
max attempts·cache(`ordered-world-subject-resolution:v4`)는 바꾸지 않은 후처리 변경이다.

이 수정 전 등록/임시 대상과 후속 근거 격리 테스트 3개가 실패했고, 수정 후 관련 테스트
32개가 통과했다. 전체 오프라인 테스트는 1,706개 통과, integration 14개 제외,
기존 Starlette 경고 1개다. `failure16-red.log`, `failure16-green.log`, `full-after-fix.log`에 보관했다.
Java 제품 코드는 수정하지 않았다. 앞선 58개 Java 회귀 검증과 이번 실제 자동 반영 결과를 구분한다.

| 실제 결과 | 집계 |
| --- | --- |
| 완료/중단 | 최종 20/20화 완료, 최초 실행 중단 1건을 수정·재개 |
| 최종 비교 실패 | 캐릭터 0, 세계관 0 |
| 후보 | 330개: 자동 확정 285, 제외 19, 직접 검토 대기 26 |
| 후속 회차 연결 | 19/19구간 선행 저장 및 실제 확정 출처 포함 확인 |
| 저장·출처·입력 hash·journal replay·보류 참고 검사 | 오류 0건 |
| 중단 없이 회복한 검증 거절 | 캐릭터 6회 + 세계관 비교 9회 = 15회 재시도 경고 |
| 공급자 사용량 | 454회, 입력 2,489,197 / 입력 중 캐시 1,034,764 / 출력 90,069 |

16화 재개는 동일 Job과 `WORLD_CANDIDATES_PUBLISHED` checkpoint를 사용했다. 1~15화 완료
기록·입력/출력 hash, 16화 고정 입력·원문 조각 2개·캐릭터 후보 9개와 비교·세계관 후보 9개를
보존했다. 재개에서 대상 연결 9회와 세계관 비교 5회만 호출했고 이미 완료한 추출을 반복하지 않았다.
전체 완료 후에도 최초 6화 관찰 시점의 후보 110개 원본과 입력/출력 hash가 일치했다.

재시도 경고는 같은 비교의 반복 시도를 포함한다. 최종 실패 15건이라는 뜻이 아니다.
세계관 최종 위치 중복, 대상/속성 선택 누락, 범위 불일치, 같은 source 중복, 새 분류의 하위 속성
부족과 캐릭터 과거/가정 반영, 비활성 상태 반영, 기존 속성 ADD, 수정 대상 속성 불일치가 있었다.
각 회차·원인·실제 재개 결과는 비공개 `report.md`와 `log-diagnostics.json`에 보관했다.
공급자 사용량의 실패 outcome 0건은 공급자 응답 후 발생한 내부 검증 오류 0건을 뜻하지 않는다.

이는 중간 수정이 포함된 20화 실행 1회다. 수정 후 코드는 16화 재개 및 17~20화에 적용했으며,
처음부터 수정된 코드로 20화를 무중단 반복 검증한 결과는 아니다. GH180 담당자가 머지 전
새 묶음의 연속 실행을 추가하고, 주체 연결의 후보별 고정 진단 및 반복 실패 격리 계약을 후속 검토한다.
정답지 기반 품질 지표는 미측정이며 별도 담당 이슈의 같은 입력·모델·judge 조건 평가가 필요하다.
v0002의 상태 반영 오류 2건을 해결했다고 주장하지 않는다. 사이트·워커는 수정된 로컬 코드로 실행 중이며,
원문 저장은 로컬 파일, DB/Redis도 로컬 전용이다. 운영 S3와 DB에는 연결하지 않았다.


## 추가 보완 — 후보 연결과 비교 준비 오류 격리 (2026-09-11)

같은 GH180 변경의 안정성 보완이며 v0004을 유지한다. 위 실제 20화 실행 기록은 수정 전/도중의
기록으로 보존하며, 이 추가 변경을 적용한 새 실제 20화 무중단 검증으로 바꾸어 해석하지 않는다.

- 자동 순차 세계관 주체 연결에서 격리 가능한 실패는 후보별 `failureCode`로 전달한다. Spring의
  `FAILED` 결과를 검증하고 대상 목록에 넣지 않는다. 명시적 모호성 처리와 실행 실패를 구분한다.
- 선택적 인물 연결 보강 실패는 원래 미해결 후보만 최초 저장 시 실패와 단계로 남긴다. 이미 규칙으로
  연결한 후보와 인물 발견은 보존한다. 자동 모드에 한정하며 수동/legacy 실패 경계는 유지한다.
- 세계관 신뢰도를 추출·매핑·게시에서 유한한 0~1로 허용하고 원값을 보존한다. 0.9를 특정 등급으로
  반올림하지 않는다. Java 게시 검증도 같은 범위로 변경했다.
- 계정 인증·권한·결제·영구 4xx 제공자 오류는 후보 실패 격리에서 제외하고 작업 실패로 보존한다.
  metering의 짧은 재시도, 모델·추론·원문 문맥·호출 상한은 변경하지 않았다.
- Spring V51과 후보 초기 저장의 단계 매핑을 함께 배포한다. Spring은 비교 준비의 항목별 실패,
  큰 세계관 묶음 분할, 비교 문맥 크기 보류, 최종 자동 반영 사전 검증과 journal을 소유한다.

AI 전체 오프라인 테스트 1,763개 통과, integration 14개 제외, 기존 Starlette 경고 1개.
소켓 차단 runner와 고정 응답을 사용했으며 실제 OpenAI 호출·추가 원고 전송·judge는 없다.
실행 로그는 상위 작업공간 `.codex-artifacts/gh180/test3-episodes-1-20/continuity-ai-full.log`.
Java·HTTP·프론트 및 기동 검증 최종 결과는 상위 `continuity-hardening-20260911/report.md`에 보완한다.
미커밋 작업 트리이므로 복원용 단일 SHA는 여전히 미확정이다. 머지 전 최종 코드 SHA를 기록해야 한다.

정확도·재현율은 미측정이다. 안정성 담당은 GH180 작업자이며 수정본의 새 실제 연속 실행과
운영 지표를 후속 확인한다. 전체 1차 추출의 부분 완료, 지연 자동 재개, 진행 중 수정 예약은
별도 상태 계약 후속 범위이며 이번 검증 결과에 포함하지 않는다.


최종 통합 확인: Java 전체 1,007개 통과(환경 조건 34개 제외), 프론트 E2E 21개 통과.
별도 빈 PostgreSQL과 실제 Spring HTTP/Python Worker에 연결 실패 2개를 주입한 **고정 응답
20회차**가 전부 자동 저장 완료됐다. 다음 회차의 확정 사실과 미해결 참고 구분, 마지막 값 저장,
보류 세계관의 사용자 수정·확정도 통과했다. 고정 응답 108회, 실제 provider 호출 0회다.
기존 전체 PostgreSQL/HTTP 시나리오를 전부 다시 실행한 결과는 아니다.
근거: 상위 `.codex-artifacts/gh180/continuity-hardening-20260911/java-fault-injection/summary.json`.

로컬 Java V51·사이트·워커 재기동 후 API UP, 사이트 200, 네 워커 코드 일치를 확인했다.
실행 코드 hash: `6aac26ee1bba8d11d6ebd3891984a230562ae90c39d0176fd17e7c3b3be34dd4`.
기존 분석 100건·회차 102개와 후보/사실/현재 설정 5개 테이블의 내용 해시를 대조하여 동일함을 확인했다.
새 nullable 준비 실패 컬럼은 해시에서 제외했다. 원고·Gold 정확도 재측정은 없다.

## 추가 보완 — 범위 외의 일반 불확실성 검토 (2026-09-11)

같은 GH180 미머지 변경의 후속 계약으로 v0004을 유지한다. 위 실행 기록과 과거 요청
baseline은 보존한다. 로직·프롬프트 변경 담당은 GH180 작업자이며 단일 복원 SHA는 미확정이다.

수정 전의 후속 실제 20회차 실행은 모두 완료됐다. 후보 454개 중 자동 확정 409개,
제외 21개, 검토 대기 24개이며 비교 실패는 캐릭터 1개·세계관 3개였다. 후보별 실패 격리 덕분에
회차 전체가 중단되지는 않았다. 세계관 세 실패는 유효한 범위 없는 기존 항목을 선택한 뒤
명시적 범위가 필요한 검토 사유를 반환해 발생했다. 실제 비교 입력과 안전한 3회 진단은
보존되어 있지만 실패 응답 본문은 없어 모델의 자유 서술 판단 이유는 복원할 수 없다.
근거: 상위 비공개 `.codex-artifacts/gh180/test3-episodes-21-40/snapshot.json` 및 로컬 worker 로그.
이 실행은 아래 일반 검토 변경을 적용하기 전이며 새 코드의 실제 LLM 검증으로 해석하지 않는다.

- `WorldSettingComparisonReviewReason.GENERAL_UNCERTAINTY`를 단건·batch·ordered 응답 계약에 추가했다.
  모델이 명시적으로 일반 불확실성을 선택하면 정상 검토로 보존하며 자동 확정하지 않는다.
- batch의 일반 검토는 source 하나씩, 이동 없음이다. 기존 target/property를 지정하면 원래 입력의
  유효한 위치인지 검사한다. 기존 scope 검토, concrete 변경의 전체 경로, 입력·lease·quota 검증은 유지한다.
- 단건 deterministic 복원을 사용해 일반 검토의 proposed 범위·설정명·값을 원본으로 보존한다.
  여러 추출값이 한 후보에 통합돼도 전체 문자열을 남긴다. 정상 scope 검토로 재분류하지 않는다.
- 사용자 판단 근거에는 내부 enum을 표시하지 않는다. “대상이나 내용을 확실히 판단하기 어려워
  확인이 필요합니다”처럼 무엇을 확인해야 하는지 자연스러운 한국어로 설명한다.
- 모델·추론 강도·추출 입력·주체 연결 prompt·호출 상한은 변경하지 않았다. 세계관 비교 캐시는
  단건 `world-setting-comparison:v11`, batch `world-setting-comparison-batch:v8`,
  ordered 추가 지침 `ordered-provisional-v6`이다.
- 고정 #65 baseline/probe 파일은 유지하고 `upstream_requests_pr65.json`의 승인된 override만
  mock transport로 재포착했다. 바뀔 수 있는 필드는 전체/system hash와 cache key로 제한하며
  user payload·schema·model·reasoning·store·출력 상한의 기존 기대값은 유지했다.

별칭과 하위 종류를 과도하게 같은 대상으로 연결해 이미 자동 반영한 품질 문제도 관찰했다.
이번 사유 추가가 그 문제를 해결하지는 않는다. 과거 실패·확정 결과를 수정하거나 새 실제
원고를 보내지 않았으며, 정답지 기반 점수는 미측정이다. 정확도 담당의 동일 조건 평가와
GH180 담당의 새 실제 연속 실행을 별도로 수행해야 한다. 검증 결과는 아래에 추가한다.

최종 오프라인 검증: AI 전체 **1,798개 통과**, integration 14개 제외, 기존 Starlette 경고 1개.
새 일반 검토 계약 34개와 실제 comparator를 사용하는 HTTP 하네스 fixture 1개를 추가했다.
기존 후보·범위·없는 참조·일반 실행 실패·golden 요청 계약을 포함한 전체 회귀가 통과했고
수정한 Python 파일 Ruff 및 `git diff --check`가 통과했다. 로그는 상위 비공개
`.codex-artifacts/gh180/general-uncertainty-20260911/ai-tests.log`에 보관한다.
실행은 clean env의 기존 socket 차단 `offline_pytest.py`를 사용했다. 외부 provider·judge 호출은 없다.

Java 담당의 별도 실제 Spring HTTP/PostgreSQL 검증에서도 합성 2회차가 완료됐다. 1화에서
정상 ADD와 일반 검토가 같은 batch에 섞여도 정상 설정만 확정되며, 2화는 확정 사실과
원래 주체의 미확정 참고를 구분해 읽었다. `GENERAL_UNCERTAINTY`는 DB/journal에 보존하고
기존 Worker 참고 DTO의 `reason`에는 자연어를 전달한다. API 필드에 내부 enum을 새로 추가하지 않았다.
통합 로그: Java `build/reports/tests/ordered-http-worker-automatic-general-uncertainty.log`.
이는 고정 응답 2회차 테스트이며 실제 LLM을 사용한 새 20회차 또는 품질 개선 측정이 아니다.


## 최신 main 및 PR65 재통합 (2026-09-13)

위의 실행들은 당시 코드와 migration 번호를 보존한 과거 기록이다. 새 PR은 main
`05d9c2d65498d28a1208b7f9bc8f0087307f64fc`를 포함한 PR65
`6deac0400c7b84a22715d669f30dfb12a7ca2b34`를 기준으로 쌓는다. 해당 변경의 공식 v0003과
main v0002를 보존하고 GH180 기록만 v0004로 옮겼다. Java의 미배포 migration도 main의
이메일 가입 V42와 구분하기 위해 V43~V53으로 옮겼다. 사용자 로컬 실험 DB는 갱신하지 않았다.

- PR67의 새 인물 평가·후보별 처리 기록·provider 허용 목록 진단·300초 읽기 제한을 유지했다.
- 연속 분석용 COMMON_START/ORDERED_PROVISIONAL의 상태 기록과 runtime CLI 옵션을 함께 보존했다.
- 계정/설정 400·401 오류는 자동 격리할 수 없는 UNEXPECTED_ERROR로 남기고 실제 HTTP·provider 진단은 유지한다. 429·5xx와 구별하는 회귀 검증을 통과했다.
- 독립 추출한 위 PR65 코드에서 합성 저장 예측을 다시 평가하여 `expected-reports-pr65-6deac04.json`을 포착했다. 예전 `expected-reports-pr65.json`은 보존한다. ORACLE/FIXED/ROLLING 전체 보고서의 새 진단까지 대조했고 외부 judge/provider는 호출하지 않았다.
- 합쳐진 오프라인 테스트 1,861개 통과, integration 14개 제외, Ruff 통과. 별도 PostgreSQL·Spring HTTP/Python 고정 응답 검증 21개 통과. 이들은 모델 정답률 측정이 아니다.

후속 실제 41~50화 감사는 최신 upstream 통합 전 로컬 코드에서 수행한 사용자 실행이다.
10 SUCCEEDED/SEALED/자동 반영, 187개 후보(169 반영/12 제외/6 세계관 검토), 최종 비교 실패 0개다.
9개 회차 인계와 10개 입력·기록 hash, 187개 후보 coverage, 이전 확정 출처 138개를 확인했다.
4개 중간 검증 실패 묶음은 재시도로 회복됐다. GENERAL_UNCERTAINTY 4개, SCOPE_MISMATCH 1개,
SCOPE_UNRESOLVED 1개를 확인했으며 주체 과병합 등 품질 과제는 별도로 남겼다.
근거는 비공개 로컬 test3-episodes-41-50 감사 기록이며 원문·개별 판단은 공개 문서에 복사하지 않는다.

재평가 계획: GH180 담당이 통합 코드의 다음 실제 연속 실행을 확인하고, 정확도 담당이 같은
Gold·모델·judge 조건에서 FIXED 평가를 수행한다. 아직 새 품질 점수나 개선 폭은 주장하지 않는다.

당시 통합 코드 복원 SHA: `457c63ba7dedf3e08a2ffa866bb7098a20980fa9`. 아래 세 번째 리뷰 수정과 회귀 테스트를 포함하며 오프라인 1,879개 및 Ruff를 통과한 코드다. 이 안내를 갱신하는 후속 커밋은 문서만 변경한다. 이전 `f43a019a15c65d3396598dd408a5d73296b4a3af`는 두 번째 리뷰의 1,868개 검증, `572d9cc7174218d853d078da3ec5a7072457b478`는 최초 통합의 1,861개 검증 기준이며 둘 다 분리 전 통합 브랜치의 조상이다. 과거 코드에는 후속 수정이 없으므로 최종 복원에는 사용하지 않는다.

당시 계획은 #65 브랜치를 base로 쓰고 머지 후 main으로 변경하는 것이었다. 이후 사용자 결정으로 이 계획을 폐기하고, 위 현재 기준처럼 #65 없이 main 위에 분리했다.
# 2026-09-13 리뷰 후속: 평가 중단 기록

COMMON_START 평가에서 provider 중단이 발생하면 원래 예외를 보존하면서 명시적 시작 상태·정책 버전·FAILED 실행 trace를 포함한 부분 결과를 첨부한다. 완료된 앞 회차 기록을 유지하고, 중단 회차의 일부 판단을 완료 상태로 적용하거나 다음 회차를 실행하지 않는다. 첫 회차/후속 회차 중단과 JSON 저장 후 재조회, 기존 평가·provider 진단 회귀를 synthetic 입력으로 검증했다.

이번 변경은 평가기 오류 기록에 한정한다. 실제 연속 분석의 입력 검증·provider 호출·복구·중단 조건은 강화하지 않는다. 출처 누락 분류 변경, 출처 회차 필수화, 추출 schema 입력량 검사는 사용자 결정으로 보류했다. 사용자 문장의 SCOPE_MISMATCH 노출은 Java #193의 공개 문장 변환으로 처리하며 AI 응답 거절과 추가 호출을 늘리지 않는다.

## 두 번째 리뷰 반영: 진단과 평가 연속성

- 순차 캐릭터 비교의 검증 오류는 허용된 필드 경로·오류 종류 요약만 로그와 최종 실패에 기록한다. 거절된 설정값이나 내부 참조를 예외 문자열에서 복사하지 않는다. 기존 canonical slot 충돌 표식과 재시도 횟수, 실패 분류는 유지한다.
- 저장된 순차 실행 평가에서 후보 실패와 회차 중단을 구분한다. 회차가 완료되고 서버 journal이 SEALED이면 일부 후보 실패를 보존한 채 다음 회차의 관찰을 읽는다. adapter·bundle 검증·평가기 세 경계에 동일한 기준을 적용했다. 회차 자체의 중단 또는 미완료 journal은 여전히 다음 회차의 기준이 될 수 없다. 실패 후보를 삭제하거나 성공으로 변환하지 않으며 실패 집계와 채점 분모에 남긴다.
- 세계관 대상의 원본 식별자와 인코딩된 식별자도 기존 일대일 이름 대응을 통해 채점용으로 변환한다. 속성 참조만 변환되어 정상 ADD가 오답이 되는 경우를 보완했다. 실제 분석 입력·응답·trace와 hash는 바꾸지 않고 모호한 이름을 점수 향상을 위해 병합하지 않는다.
- 오프라인 전체 **1,868개 통과**, 외부 PostgreSQL integration 14개 제외. 합성 오류의 민감 값 비노출, 부분 실패를 포함한 완료 회차의 JSON 왕복·후속 회차 평가, 대상 참조 두 형식의 채점용 변환과 원본 보존을 포함한다. 기존 Starlette 경고 1개 외 신규 경고는 없다. 변경 Python의 Ruff 통과. 외부 provider·judge 호출이나 실제 원고 분석은 수행하지 않았다.
- 세계관 후보 출처 회차 필수 검증은 보류한다. 누락의 문제는 남아 있지만 전체 요청 거절을 추가하면 연속 분석 중단이 늘 수 있다. 향후 서버 전달 경계와 영향 후보만 보류하는 방식으로 검증한다. 앞서 보류한 사항도 유지한다.

## 세 번째 리뷰 반영: 중복 사실과 재개 표시

- 순차 캐릭터 저장은 같은 실제/임시 인물, 같은 회차 원문, 항목·타입·JSON 값, 정확히 같은 원문 근거 위치와 인용이 모두 일치하는 SETTING만 중복 제거한다. 겹친 청크에서 같은 위치를 재추출한 경우 높은 신뢰도의 후보와 연결 ID를 유지한다. 위치가 없거나 다름, 미해결 연결, 다른 인물·값·근거는 보존한다. 이름이나 값만 같다는 이유로 다른 시점의 사실을 지우지 않으며 발견 후보의 anchor도 바꾸지 않는다.
- 이미 비교 완료 checkpoint까지 도달한 재개는 남은 후보만 계속 claim하되 과거 단계로 progress를 되돌려 보고하지 않는다. 완료 요청 실패·재개 및 기존 확정 설정 모드도 검증했다.
- 실패한 저장 평가 trace는 입력·출력 상태와 Backend hash가 같고 적용 event가 없어야 한다. 조작된 실패 trace가 미확정 변경을 평가 결과로 포함하던 경우를 차단했다. 실제 서비스 Worker 입력이나 중단 조건에는 적용하지 않는 평가 자료 검증이다. SEALED 완료 회차의 개별 후보 실패는 계속 보존하고 다음 회차를 평가한다.
- AI 오프라인 **1,879개 통과**, 외부 integration 14개 제외, Ruff 통과. 실제 원고·외부 provider/judge 호출은 없다. 이번 중복 처리의 정답률 개선 수치는 측정하지 않았다.
- 세계관 이름 exact 연결과 해소 전 전체 경로 통합 제안은 보류한다. 순차 모드에는 동명이지만 다른 존재를 원문 근거로 구분하는 별도 계약·회귀가 있다. 기존 이름 기반 함수를 무조건 재사용하면 이 구분을 없앤다. 이름만으로 연결하거나 20개 초과 시 새 중단 조건을 추가하지 않으며, 후속 중복 최적화는 동일 주체 해소 뒤 원문 시점을 보존하는 범위로 평가한다. 일반 규칙과 순차 예외를 혼동하지 않도록 이 결정을 기록한다.
